services:
  app:
    build: .
    ports:
      - "8083:3000"
    environment:
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://localhost:8083}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:-change-this-secret-in-production}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:8083}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
